{
  "name": "durable-streams-effect",
  "version": "0.1.0",
  "description": "Effect-based server implementation of the Durable Streams Protocol",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run conformance",
    "test:watch": "vitest conformance",
    "bench": "vitest bench benchmarks"
  },
  "dependencies": {
    "@effect/platform": "^0.75.0",
    "@effect/platform-node": "^0.70.0",
    "effect": "^3.15.0"
  },
  "devDependencies": {
    "@durable-streams/benchmarks": "^0.2.3",
    "@durable-streams/server-conformance-tests": "^0.3.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
